# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
bin/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the Python version is actually
#   packaged, with the package. However, with an app it can be useful to include this.
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or even
#   fail to install them. In that case, the lock file should be added to .gitignore.
#Pipfile.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   poetry.lock
#   .python-version

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#   pdm.lock
#   .pdm-python

# PEP 582; used by e.g. pdm.
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/
env.yaml
# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#  JetBrains specific template is maintained in a separate repository
#  https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file. For a shared
#  project it is usually better to keep it separate.
#  .idea/

# VS Code
.vscode/

shortcuts.md
documentation.md
# Google Cloud Credentials
gen-lang-client-*.json
*.json
!cors-config.json
!package.json
!tsconfig.json
!next.config.json
# PR-4: the golden rubric fixtures are SOURCE (the frontend vitest + Playwright
# suites read them in place) — not eval output. The broad *.json above would drop
# them, breaking CI on a fresh checkout.
!tests/rubric_eval_suite/benchmarks/*.json
# Grading eval suite (mission v0, same precedent): configs, per-fixture
# manifests, frozen contract snapshots and GT files are SOURCE — hash-pinned
# provenance the runner validates against committed state. NOT eval output.
!tests/grading_eval_suite/configs/*.json
!tests/grading_eval_suite/fixtures/*.json
!tests/grading_eval_suite/benchmarks/**/*.json
!tests/grading_eval_suite/plans/**/*.json
# ...except the F0 staging area (superseded by the ratified snapshot):
tests/grading_eval_suite/benchmarks/contracts/_proposed/
# Transcription eval suite (same precedent): the EXAM SPECS, per-fixture
# manifests and pipeline configs are SOURCE — they are the ruler a run was
# scored against, and results.json pins them by sha256. Without these a fresh
# clone cannot reproduce any run, and check_goal.sh's default --exam-spec
# names a file that does not exist. NOT eval output.
!tests/transcription_eval_suit/exams/*.json
!tests/transcription_eval_suit/fixtures/*.json
!tests/transcription_eval_suit/configs/*.json
!tests/transcription_eval_suit/draft.json
# eval RUN OUTPUTS are regenerable artifacts — never commit them (repo bloat).
tests/*/results/
annotated-docx*

transcription_rnd/

# =============================================================================
# Project-specific ignores
# =============================================================================

# Debug directories (local development only)
debug_handwritten_pages/
debug_vlm_responses/

# Deprecated/archived code
deprecated/

# Test scripts (not needed for deployment)
debug_single_page.py
local_test_vision.py
test_enhanced_rubric_extraction.py
test_enhanced_verification.py
test_pdf_gen.py

# Test/sample PDF files (large binary files)
*.pdf
!migrations/*.pdf

# Manual test transcripts
manual_test_transcript.txt
app/tests_manual_transcription.txt

# Local documentation (not needed for deployment)
handwritten_transcription.md
STRUCTURE.md
shortcuts.md
documentation.md

# Temp/cache directories
temp/
logs/
.cache/

# IDE/Editor specific
.idea/
*.swp
*.swo
*~

# OS specific
.DS_Store
Thumbs.db
